name: Reviews.io summary
machineName: reviews_widget
status: true
props:
  properties:
    # --- Tab 1 ---
    backgroundColor:
      title: Background colour
      type: string
      examples:
        - '#ffffff'
    padding:
      title: Padding
      type: string
      enum:
        - None
        - Apply padding to component
      examples:
        - Apply padding to component
    conditionallyApplyPaddingIfBackgroundColorIsSet:
      title: Conditionally apply padding if background color is set
      type: string
      examples:
        - ''
