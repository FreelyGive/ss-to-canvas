name: Modal button
machineName: modal_button
status: true
props:
  properties:
    # --- Tab 1 ---
    buttonId:
      title: Button ID
      type: string
      examples:
        - open-modal-btn
    buttonText:
      title: Button text
      type: string
      examples:
        - Open Modal
    buttonStyle:
      title: Button style
      type: string
      enum:
        - Color button
        - Dark button
        - Light button
      examples:
        - Color button
    modalWindowId:
      title: Modal window ID
      type: string
      examples:
        - my-modal
