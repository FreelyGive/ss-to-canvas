name: Process Steps
machineName: process_steps
status: true
required:
  - preHeading
  - heading
props:
  properties:
    preHeading:
      title: Pre Heading
      type: string
      examples:
        - How we keep pricing fair
    heading:
      title: Heading
      type: string
      examples:
        - We cut out the middleman, not the quality.
    text:
      title: Body Text
      type: string
      contentMediaType: text/html
      x-formatting-context: block
    videoUrl:
      title: Video URL (mp4)
      type: string
      examples:
        - https://example.com/video.mp4
    leftColumnTitle:
      title: Left Column Title
      type: string
      examples:
        - The old way
    leftColumnFooter:
      title: Left Column Footer Note
      type: string
    rightColumnTitle:
      title: Right Column Title
      type: string
      examples:
        - The IOLLA way
    rightColumnFooter:
      title: Right Column Footer Note
      type: string
slots:
  leftSteps:
    title: Left Steps
  rightSteps:
    title: Right Steps
