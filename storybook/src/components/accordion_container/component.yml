name: Accordion container
machineName: accordion_container
status: true
props:
  properties:
    # --- Tab 1 ---
    navigationStyle:
      title: Select
      type: string
      enum:
        - Dark text with key-line
        - Light text with key-line
        - Solid (For any background)
      examples:
        - Dark text with key-line
    backgroundColor:
      title: Background color
      type: string
      examples:
        - '#ffffff'
    addSpaceBelow:
      title: Select
      type: string
      enum:
        - None
        - Add space below
      examples:
        - Add space below
slots:
  items:
    title: Accordion Items
